/**
 * @file secure_services.c
 * @brief Secure service initialization implementation.
 *
 * Secure services will eventually host secure-only state and non-secure callable
 * gateways. The current scaffold keeps the boot sequence stable while services
 * are incrementally added.
 */

#include "nexus_secure_services.h"

void nexus_secure_services_init(void)
{
}
